# Chatness Svelte

This example was built using [create-svelte-app](https://www.npmjs.com/package/create-svelte-app)

## Setup

- npm install -g create-svelte-app
- clone this repo
- cd svelte
- svelte serve
- svelte build
